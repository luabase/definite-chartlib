export enum ChartType {
  BAR = "bar",
  CALENDAR = "calendar",
  HEATMAP = "heatmap",
  LINE = "line",
  PIE = "pie",
  SCATTER = "scatter",
}

export enum DataType {
  CATEGORY = "category",
  DATETIME = "datetime",
  VALUE = "value",
}

export enum AggregationType {
  AVG = "avg",
  COUNT = "count",
  DISTINCT = "distinct",
  SUM = "sum",
  MIN = "min",
  MAX = "max",
  NONE = "none",
}

export enum AxisType {
  LEFT = "left",
  RIGHT = "right",
}

export enum OrientationType {
  VERTICAL = "vertical",
  HORIZONTAL = "horizontal",
}

export enum BarStyleType {
  GROUPED = "grouped",
  STACKED = "stacked",
}

export enum LineStyleType {
  POINT = "point",
  SMOOTH = "smooth",
}

export enum ColorGroupingType {
  CONTINUOUS = "continuous",
  PIECEWISE = "piecewise",
}
